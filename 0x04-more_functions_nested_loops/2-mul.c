#include "main.h"

/**
 *mul - this is the main function
 *@a: first parameter of the function
 *@b: second parameter of the function
 *Return: parameter a and b
 */
int mul(int a, int b)
{
	int i = (a * b);

	return (i);
}
